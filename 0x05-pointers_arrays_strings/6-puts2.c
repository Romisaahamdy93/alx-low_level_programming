#include "main.h"
/**
 * puts2 -  prints every other character of a string
 *
 * @str: is the parameter to check
 *
 * Return: Always 0 (Success)
 */
void puts2(char *str)
{
int x;
int y = 0;
while (str[x] != '\0')
{
x++;
}
for (y = 0; y < j; y += 2)
{
_putchar(str[y]);
}
_putchar('\n');
}
